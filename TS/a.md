tsc -w (watch mode running in background)
npm i lite-server (similar to nodemon)

## Tuples

enums
interfaces
interface vs type
classes
getters and setters
protected
abstracted class = cant create a object
generics
